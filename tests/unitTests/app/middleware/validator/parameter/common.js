const common = {
  Parameter: [
    { name: 'Must be a valid non-empty text value' },
    { isRequired: 'Must be a true/false boolean value' },
    { tooltip: 'Must be a valid non-empty text value' },
    { type: 'Must be a valid non-empty text value' },
  ],
};
module.exports = common;
